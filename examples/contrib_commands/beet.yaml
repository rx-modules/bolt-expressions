name: bolt-expressions-commands

data_pack:
  pack_format: 10
  load:
    data/test/functions: src

require:
  - bolt_expressions
  - bolt_expressions.contrib.commands
  - bolt

pipeline:
  - mecha

output: dist

meta:
  generate_namespace: test
